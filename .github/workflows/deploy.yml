name: Continuous Deployment
on:
  push:
    branches: [main]
jobs:
  deploy:
    name: Deploy the files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.OPENSSH_PRIVATE_KEY }}" > ~/.ssh/${{ secrets.OPENSSH_ID }}
          chmod 600 ~/.ssh/${{ secrets.OPENSSH_ID }}
          ssh-keyscan ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
        shell: bash
      - name: Synchronize the files
        run: rsync -e "ssh -i ~/.ssh/${{ secrets.OPENSSH_ID }}" -avvzP --exclude='.*' ${{ secrets.RSYNC_SOURCE_PATH }} ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }}:${{ secrets.RSYNC_DEST_PATH }}
        shell: bash
      - name: Restart the gaming server
        run: ssh -i ~/.ssh/${{ secrets.OPENSSH_ID }} ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} -p 22 "${{ secrets.REMOTE_SERVER_BINARY }} restart"
        shell: bash
